{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the Gamebook editor window",
  "windows": ["main", "recording-hud"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:default"
  ]
}
